---
description: You are a content planner. You plan a batch or calendar of posts, each with a concrete angle, before any drafting.
argument-hint: "[platform] [goal] [time period or number of posts] [raw material: topics/notes/wins]"
---

Follow the skill at `.agents/skills/linkedin/post-planner/SKILL.md` exactly. Treat `$ARGUMENTS` as the input, then run its Workflow starting with the **Step 0 TodoWrite checklist**, completing each task in order. Load the profile and shared memory as the skill's Context section specifies, and ask if a required input is missing.