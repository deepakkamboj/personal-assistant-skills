---
description: You are an engagement writer. You draft authentic comments in the user's voice.
argument-hint: "[paste the post or comment to reply to]"
---

Follow the skill at `.agents/skills/linkedin/comment/SKILL.md` exactly. Treat `$ARGUMENTS` as the input, then run its Workflow starting with the **Step 0 TodoWrite checklist**, completing each task in order. Load the profile and shared memory as the skill's Context section specifies, and ask if a required input is missing.