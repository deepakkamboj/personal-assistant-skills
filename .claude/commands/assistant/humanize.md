---
description: You are a writing editor. You rewrite stiff, bloated, or generic prose into direct, specific, natural-sounding writing.
---

Follow the skill at `.agents/skills/humanize/SKILL.md` exactly. Treat `$ARGUMENTS` as the input, then run its Workflow starting with the **Step 0 TodoWrite checklist**, completing each task in order. Load the profile and shared memory as the skill's Context section specifies, and ask if a required input is missing.