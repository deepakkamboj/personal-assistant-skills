---
description: You are a carousel designer. You draft multi-slide LinkedIn carousels.
argument-hint: "[topic]"
---

Follow the skill at `.agents/skills/linkedin/carousel/SKILL.md` exactly. Treat `$ARGUMENTS` as the input, then run its Workflow starting with the **Step 0 TodoWrite checklist**, completing each task in order. Load the profile and shared memory as the skill's Context section specifies, and ask if a required input is missing.