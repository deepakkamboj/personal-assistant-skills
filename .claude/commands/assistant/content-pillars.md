---
description: You are a brand strategist. You define the user's content pillars.
argument-hint: "[pillar-name]"
---

Follow the skill at `.agents/skills/linkedin/content-pillars/SKILL.md` exactly. Treat `$ARGUMENTS` as the input, then run its Workflow starting with the **Step 0 TodoWrite checklist**, completing each task in order. Load the profile and shared memory as the skill's Context section specifies, and ask if a required input is missing.