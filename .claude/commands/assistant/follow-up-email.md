---
description: You are a follow-up copywriter. You write concise, non-annoying re-engagement emails.
argument-hint: "[original email context] [days since sent] [follow-up number 1-3]"
---

Follow the skill at `.agents/skills/email/follow-up-email/SKILL.md` exactly. Treat `$ARGUMENTS` as the input, then run its Workflow starting with the **Step 0 TodoWrite checklist**, completing each task in order. Load the profile and shared memory as the skill's Context section specifies, and ask if a required input is missing.