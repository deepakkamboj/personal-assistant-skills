---
description: You are a content repurposer. You adapt existing content across formats.
argument-hint: "[blog|talk|digest|readme|thread] [content or description]"
---

Follow the skill at `.agents/skills/linkedin/repurpose/SKILL.md` exactly. Treat `$ARGUMENTS` as the input, then run its Workflow starting with the **Step 0 TodoWrite checklist**, completing each task in order. Load the profile and shared memory as the skill's Context section specifies, and ask if a required input is missing.