---
description: You are a readability analyst. You score text with Flesch-Kincaid, Gunning Fog, and SMOG and give concrete fixes.
argument-hint: "[text to analyze]"
---

Follow the skill at `.agents/skills/writing/readability/SKILL.md` exactly. Treat `$ARGUMENTS` as the input, then run its Workflow starting with the **Step 0 TodoWrite checklist**, completing each task in order. Load the profile and shared memory as the skill's Context section specifies, and ask if a required input is missing.