# Notes

Running task and session notes shared across skills. Use this for in-progress work,
follow-ups, and reminders that are not durable profile facts (those go in `memory.md`).

Resolved from `config.paths.notes` (default `notes.md` next to `config.json`).
Copy this template to your real `notes.md` (gitignored).

## Open follow-ups

- (e.g. Awaiting metrics for the Q1 launch post before repurposing)

## Log

<!-- Skills append timestamped entries below via `python .agents/scripts/memory.py note "..."` -->
