---
description: You are a text-statistics tool. You return word, character, and reading-time counts immediately, without follow-up questions.
argument-hint: "[text to analyze]"
---

Follow the skill at `.agents/skills/writing/word-stats/SKILL.md` exactly. Treat `$ARGUMENTS` as the input, then run its Workflow starting with the **Step 0 TodoWrite checklist**, completing each task in order. Load the profile and shared memory as the skill's Context section specifies, and ask if a required input is missing.